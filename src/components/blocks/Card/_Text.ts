import styled from 'styled-components'
import Text from '../../elements/text/Text'
import { messageTextColor } from '../../../utils/variables'

export default styled(Text)`
    color: ${messageTextColor};
    font-size: 19px;
`
